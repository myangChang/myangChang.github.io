<?xml version="1.0" encoding="utf-8"?>
<search>
  
  
  
  <entry>
    <title>高并发抢单系统设计：从 Redis 原子判重到 MySQL 最终一致性的 Java 实践</title>
    <link href="/2026/09/16/high-concurrency-order-grabbing-system-design/"/>
    <url>/2026/09/16/high-concurrency-order-grabbing-system-design/</url>
    
    <content type="html"><![CDATA[<p>抢单系统的难点，不是把 <code>UPDATE</code> 语句写得足够快，而是让几十万请求同时到达时，系统仍然满足三个约束：<strong>一个订单最多一个赢家、用户重试不会重复抢、最终结果可追踪、可修复</strong>。</p><p>这篇文章给出一套可以直接落地的 Java 技术方案：Redis 负责瞬时裁决，Redis Stream 或消息队列负责串行化写入，MySQL 负责最终事实，定时任务负责最终一致性。文中的代码以 Spring Boot + Lettuce + Redis Cluster + MySQL 为例。</p><span id="more"></span><h2 id="一、先明确抢单的语义"><a href="#一、先明确抢单的语义" class="headerlink" title="一、先明确抢单的语义"></a>一、先明确抢单的语义</h2><p>不同业务里的“抢单”可能有两种模型：</p><table><thead><tr><th>模型</th><th>示例</th><th>核心约束</th></tr></thead><tbody><tr><td>单资源归属</td><td>一个外卖订单只能被一个骑手抢到</td><td>同一个 <code>orderId</code> 只能有一个赢家</td></tr><tr><td>批量库存</td><td>一百张优惠券被很多用户抢</td><td>成功数量不能超过库存，且同一用户可配置限购</td></tr></tbody></table><p>下面的方案以单资源归属为主线，批量库存只需要把“订单状态”替换成“活动库存 + 成功集合”，核心思想完全一致。</p><p>一个可接受的用户侧流程是：</p><ol><li>用户点击抢单，服务端快速返回 <code>202 Accepted</code> 和 <code>claimId</code>；</li><li>页面显示“排队中”，轮询或通过 WebSocket 获取结果；</li><li>只有 MySQL 事务提交后，才把状态推进到 <code>ASSIGNED</code>；</li><li>重复请求使用同一个幂等键，拿回同一个结果。</li></ol><p>这里的强一致对象不是“所有请求立即看到最终状态”，而是“<strong>不重复归属、不超卖、最终可修复</strong>”。对抢单这种极短窗口的场景，先把最热的判断放到 Redis，再用消息系统削峰，是更合理的取舍。</p><h2 id="二、容量估算与目标"><a href="#二、容量估算与目标" class="headerlink" title="二、容量估算与目标"></a>二、容量估算与目标</h2><p>假设一次活动有 10 万用户同时抢 5 千个订单，峰值请求可能达到 20 万 QPS。真正会进入数据库的请求，只有 Redis 裁决成功的 5 千个左右，失败请求应该在 Redis 层直接结束。</p><p>可以给系统定一组可验证的目标：</p><table><thead><tr><th>指标</th><th>建议目标</th></tr></thead><tbody><tr><td>网关 P99</td><td>小于 10 ms</td></tr><tr><td>Redis 裁决 P99</td><td>小于 20 ms</td></tr><tr><td>用户拿到排队结果</td><td>小于 300 ms</td></tr><tr><td>最终归属确认</td><td>正常时 1 s 内，异常时可降级</td></tr><tr><td>重复归属</td><td>0</td></tr><tr><td>超卖</td><td>0</td></tr><tr><td>已受理但永久丢失</td><td>0，通过补偿任务修复</td></tr></tbody></table><p>容量上不要平均分配：热点订单和普通订单的请求密度可能相差几个数量级。Redis Cluster 至少做多分片、多副本，客户端使用 Lettuce，并设置合理的命令超时、重连和拓扑刷新参数。数据库连接池不能因为 Redis 快就无限放大，否则数据库故障时会把压力转成连接风暴。</p><h2 id="三、总体架构"><a href="#三、总体架构" class="headerlink" title="三、总体架构"></a>三、总体架构</h2><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><code class="hljs text">Client<br>  |<br>  v<br>CDN/WAF -&gt; Spring Cloud Gateway<br>              | 鉴权、限流、参数校验<br>              v<br>        Claim Service（无状态）<br>              | EVALSHA<br>              v<br>        Redis Cluster<br>        ├─ 订单裁决状态<br>        ├─ 幂等记录<br>        └─ 抢单事件 Stream<br>              |<br>              | consumer group<br>              v<br>        订单消费者 / MySQL Shard<br>              |<br>              v<br>         Kafka / WebSocket / 推送<br></code></pre></td></tr></table></figure><p>各层职责要划清楚：</p><ul><li>网关只做鉴权、基础参数校验、限流和流量染色，不查 MySQL；</li><li>抢单服务只做 Redis 原子裁决，不直接在热点路径上更新订单表；</li><li>Redis Stream 或 Kafka 负责把已受理事件可靠地交给消费者；</li><li>消费者在事务中写 MySQL，成功后更新 Redis 最终状态；</li><li>通知系统只消费数据库确认后的事件，不能把“Redis 赢了”直接当成最终成功。</li></ul><h3 id="为什么不用数据库行锁硬扛"><a href="#为什么不用数据库行锁硬扛" class="headerlink" title="为什么不用数据库行锁硬扛"></a>为什么不用数据库行锁硬扛</h3><p>如果每个请求执行：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs sql"><span class="hljs-keyword">UPDATE</span> trade_order<br><span class="hljs-keyword">SET</span> winner_user_id <span class="hljs-operator">=</span> ?<br><span class="hljs-keyword">WHERE</span> id <span class="hljs-operator">=</span> ? <span class="hljs-keyword">AND</span> winner_user_id <span class="hljs-keyword">IS</span> <span class="hljs-keyword">NULL</span>;<br></code></pre></td></tr></table></figure><p>同一订单的所有请求都会竞争同一行锁。QPS 高时，大量线程堆积在数据库连接池和 InnoDB 锁等待上，Redis、网关再快也无法解决问题。这个 SQL 不是错的，它适合作最后一道防线，但不适合作为入口裁决。</p><p>同样，不建议用 <code>synchronized</code>、本地锁或单个 Redis 分布式锁来解决热点订单问题。前者只能锁住单个 JVM，后者会把所有请求串行化，并且还要处理锁超时、续期和故障恢复。</p><h2 id="四、Redis-数据结构设计"><a href="#四、Redis-数据结构设计" class="headerlink" title="四、Redis 数据结构设计"></a>四、Redis 数据结构设计</h2><p>Redis 里的每个订单需要保存一组状态。为了兼容 Redis Cluster，所有参与同一个 Lua 脚本的 key 必须落在同一个哈希槽中，因此用分区号作为 hash tag：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs text">order:&#123;p:12&#125;:10086:state<br>claim:stream:&#123;p:12&#125;<br>claim:idem:&#123;p:12&#125;:user_7:request_abc<br></code></pre></td></tr></table></figure><p>其中 <code>p:12</code> 是根据订单 ID 计算出的逻辑分区，例如 <code>Math.floorMod(Long.hashCode(orderId), 64)</code>。订单状态、事件流和幂等键使用同一个 tag，Lua 脚本才可以在集群环境下执行。</p><p>订单状态 Hash 至少包含以下字段：</p><table><thead><tr><th>字段</th><th>含义</th></tr></thead><tbody><tr><td><code>status</code></td><td><code>AVAILABLE</code>、<code>CLAIMING</code>、<code>ASSIGNED</code>、<code>SOLD</code>、<code>CLOSED</code>、<code>FAILED</code></td></tr><tr><td><code>winner</code></td><td>当前赢家用户 ID</td></tr><tr><td><code>requestId</code></td><td>当前请求 ID，用于识别幂等重试</td></tr><tr><td><code>claimId</code></td><td>全局追踪 ID</td></tr><tr><td><code>leaseUntil</code></td><td>处理租约截止时间，防止消费者异常时永久卡住</td></tr><tr><td><code>version</code></td><td>状态版本，防止旧消费者覆盖新状态</td></tr></tbody></table><p>状态流转必须单向：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs text">AVAILABLE -&gt; CLAIMING -&gt; ASSIGNED<br>                  |<br>                  +-&gt; FAILED / RELEASED<br></code></pre></td></tr></table></figure><p><code>sold</code> 不是业务成功状态，而是“这个订单已经被别人锁定或已经售出”的外部结果。<code>AVAILABLE</code> 只表示 Redis 当前允许尝试，不等于数据库已经成功分配。</p><h2 id="五、Lua：一次原子裁决完成四件事"><a href="#五、Lua：一次原子裁决完成四件事" class="headerlink" title="五、Lua：一次原子裁决完成四件事"></a>五、Lua：一次原子裁决完成四件事</h2><p>Lua 脚本的价值不是“看起来高级”，而是让下面这些动作在一次 Redis 原子执行中完成：</p><ol><li>判断订单是否可抢；</li><li>判断是不是同一个幂等请求；</li><li>写入 <code>CLAIMING</code> 和赢家信息；</li><li>将事件写入 Stream。</li></ol><p>核心脚本可以写成这样：</p><figure class="highlight lua"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br></pre></td><td class="code"><pre><code class="hljs lua"><span class="hljs-comment">-- KEYS[1] order state</span><br><span class="hljs-comment">-- KEYS[2] claim stream</span><br><span class="hljs-comment">-- KEYS[3] idempotency key</span><br><span class="hljs-comment">-- ARGV[1] userId</span><br><span class="hljs-comment">-- ARGV[2] requestId</span><br><span class="hljs-comment">-- ARGV[3] claimId</span><br><span class="hljs-comment">-- ARGV[4] nowMillis</span><br><span class="hljs-comment">-- ARGV[5] leaseMillis</span><br><span class="hljs-comment">-- ARGV[6] orderId</span><br><br><span class="hljs-keyword">local</span> state = redis.call(<span class="hljs-string">&#x27;HMGET&#x27;</span>, KEYS[<span class="hljs-number">1</span>], <span class="hljs-string">&#x27;status&#x27;</span>, <span class="hljs-string">&#x27;winner&#x27;</span>, <span class="hljs-string">&#x27;requestId&#x27;</span>, <span class="hljs-string">&#x27;claimId&#x27;</span>)<br><span class="hljs-keyword">local</span> <span class="hljs-built_in">status</span> = state[<span class="hljs-number">1</span>]<br><br><span class="hljs-keyword">if</span> <span class="hljs-built_in">status</span> == <span class="hljs-string">&#x27;ASSIGNED&#x27;</span> <span class="hljs-keyword">or</span> <span class="hljs-built_in">status</span> == <span class="hljs-string">&#x27;CLAIMING&#x27;</span> <span class="hljs-keyword">then</span><br>  <span class="hljs-keyword">if</span> state[<span class="hljs-number">2</span>] == ARGV[<span class="hljs-number">1</span>] <span class="hljs-keyword">and</span> state[<span class="hljs-number">3</span>] == ARGV[<span class="hljs-number">2</span>] <span class="hljs-keyword">then</span><br>    <span class="hljs-keyword">return</span> <span class="hljs-number">2</span> <span class="hljs-comment">-- 幂等重试，返回已受理</span><br>  <span class="hljs-keyword">end</span><br>  <span class="hljs-keyword">return</span> <span class="hljs-number">0</span> <span class="hljs-comment">-- 已被别人抢走</span><br><span class="hljs-keyword">end</span><br><br><span class="hljs-keyword">if</span> <span class="hljs-built_in">status</span> ~= <span class="hljs-string">&#x27;AVAILABLE&#x27;</span> <span class="hljs-keyword">then</span><br>  <span class="hljs-keyword">return</span> <span class="hljs-number">0</span><br><span class="hljs-keyword">end</span><br><br><span class="hljs-keyword">local</span> oldIdem = redis.call(<span class="hljs-string">&#x27;GET&#x27;</span>, KEYS[<span class="hljs-number">3</span>])<br><span class="hljs-keyword">if</span> oldIdem <span class="hljs-keyword">then</span><br>  <span class="hljs-keyword">if</span> oldIdem == ARGV[<span class="hljs-number">3</span>] <span class="hljs-keyword">then</span><br>    <span class="hljs-keyword">return</span> <span class="hljs-number">2</span><br>  <span class="hljs-keyword">end</span><br>  <span class="hljs-keyword">return</span> <span class="hljs-number">3</span> <span class="hljs-comment">-- 同一个幂等键被用于不同请求</span><br><span class="hljs-keyword">end</span><br><br>redis.call(<span class="hljs-string">&#x27;HSET&#x27;</span>, KEYS[<span class="hljs-number">1</span>],<br>  <span class="hljs-string">&#x27;status&#x27;</span>, <span class="hljs-string">&#x27;CLAIMING&#x27;</span>,<br>  <span class="hljs-string">&#x27;winner&#x27;</span>, ARGV[<span class="hljs-number">1</span>],<br>  <span class="hljs-string">&#x27;requestId&#x27;</span>, ARGV[<span class="hljs-number">2</span>],<br>  <span class="hljs-string">&#x27;claimId&#x27;</span>, ARGV[<span class="hljs-number">3</span>],<br>  <span class="hljs-string">&#x27;leaseUntil&#x27;</span>, <span class="hljs-built_in">tostring</span>(<span class="hljs-built_in">tonumber</span>(ARGV[<span class="hljs-number">4</span>]) + <span class="hljs-built_in">tonumber</span>(ARGV[<span class="hljs-number">5</span>])))<br><br>redis.call(<span class="hljs-string">&#x27;SET&#x27;</span>, KEYS[<span class="hljs-number">3</span>], ARGV[<span class="hljs-number">3</span>], <span class="hljs-string">&#x27;EX&#x27;</span>, <span class="hljs-number">86400</span>)<br>redis.call(<span class="hljs-string">&#x27;XADD&#x27;</span>, KEYS[<span class="hljs-number">2</span>], <span class="hljs-string">&#x27;*&#x27;</span>,<br>  <span class="hljs-string">&#x27;orderId&#x27;</span>, ARGV[<span class="hljs-number">6</span>],<br>  <span class="hljs-string">&#x27;userId&#x27;</span>, ARGV[<span class="hljs-number">1</span>],<br>  <span class="hljs-string">&#x27;requestId&#x27;</span>, ARGV[<span class="hljs-number">2</span>],<br>  <span class="hljs-string">&#x27;claimId&#x27;</span>, ARGV[<span class="hljs-number">3</span>])<br><br><span class="hljs-keyword">return</span> <span class="hljs-number">1</span> <span class="hljs-comment">-- 首次受理</span><br></code></pre></td></tr></table></figure><p>返回值不要直接映射成 HTTP 成功或失败，而应该映射成稳定的业务状态：</p><ul><li><code>1</code>：首次受理，返回 <code>202 + QUEUED</code>；</li><li><code>2</code>：同一请求重试，返回同一个 <code>claimId</code> 和当前状态；</li><li><code>0</code>：订单不可抢，返回 <code>409</code>；</li><li><code>3</code>：幂等键冲突，返回参数错误或要求客户端换键。</li></ul><p>有几个生产细节必须注意：</p><ul><li>Lua 脚本中的多个 key 必须同槽，否则 Redis Cluster 会直接报错；</li><li>Redis Lua 出错时不会像数据库事务那样整体回滚，脚本越短越安全；</li><li>不要在脚本里遍历大集合，也不要把 <code>KEYS</code> 当业务查询；</li><li>使用 <code>EVALSHA</code> 执行，Spring 的 <code>DefaultRedisScript</code> 会自动处理脚本缓存和降级；</li><li>订单状态预热失败时，宁可拒绝请求，也不要让脚本把未知状态当成可抢。</li></ul><h2 id="六、Java-侧如何调用"><a href="#六、Java-侧如何调用" class="headerlink" title="六、Java 侧如何调用"></a>六、Java 侧如何调用</h2><p>抢单接口可以设计成异步受理接口，不要把 Redis 裁决包装成“最终成功”：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-meta">@PostMapping(&quot;/api/v1/orders/&#123;orderId&#125;/claims&quot;)</span><br><span class="hljs-keyword">public</span> <span class="hljs-type">ClaimResponse</span> <span class="hljs-title function_">claim</span><span class="hljs-params">(</span><br><span class="hljs-params">        <span class="hljs-meta">@PathVariable</span> <span class="hljs-type">long</span> orderId,</span><br><span class="hljs-params">        <span class="hljs-meta">@RequestHeader(&quot;Idempotency-Key&quot;)</span> String requestId,</span><br><span class="hljs-params">        Authentication authentication)</span> &#123;<br><br>    <span class="hljs-type">long</span> <span class="hljs-variable">userId</span> <span class="hljs-operator">=</span> currentUserId(authentication);<br>    <span class="hljs-type">int</span> <span class="hljs-variable">partition</span> <span class="hljs-operator">=</span> Math.floorMod(Long.hashCode(orderId), <span class="hljs-number">64</span>);<br>    <span class="hljs-type">String</span> <span class="hljs-variable">tag</span> <span class="hljs-operator">=</span> <span class="hljs-string">&quot;&#123;p:&quot;</span> + partition + <span class="hljs-string">&quot;&#125;&quot;</span>;<br><br>    <span class="hljs-type">String</span> <span class="hljs-variable">stateKey</span> <span class="hljs-operator">=</span> <span class="hljs-string">&quot;order:&quot;</span> + tag + <span class="hljs-string">&quot;:&quot;</span> + orderId + <span class="hljs-string">&quot;:state&quot;</span>;<br>    <span class="hljs-type">String</span> <span class="hljs-variable">streamKey</span> <span class="hljs-operator">=</span> <span class="hljs-string">&quot;claim:stream:&quot;</span> + tag;<br>    <span class="hljs-type">String</span> <span class="hljs-variable">idemKey</span> <span class="hljs-operator">=</span> <span class="hljs-string">&quot;claim:idem:&quot;</span> + tag + <span class="hljs-string">&quot;:&quot;</span> + userId + <span class="hljs-string">&quot;:&quot;</span> + requestId;<br>    <span class="hljs-type">String</span> <span class="hljs-variable">claimId</span> <span class="hljs-operator">=</span> UUID.randomUUID().toString();<br><br>    <span class="hljs-type">Long</span> <span class="hljs-variable">code</span> <span class="hljs-operator">=</span> redisTemplate.execute(<br>            CLAIM_SCRIPT,<br>            List.of(stateKey, streamKey, idemKey),<br>            String.valueOf(userId),<br>            requestId,<br>            claimId,<br>            String.valueOf(System.currentTimeMillis()),<br>            String.valueOf(<span class="hljs-number">10_000</span>),<br>            String.valueOf(orderId));<br><br>    <span class="hljs-keyword">return</span> <span class="hljs-keyword">switch</span> (code.intValue()) &#123;<br>        <span class="hljs-keyword">case</span> <span class="hljs-number">1</span> -&gt; ClaimResponse.queued(claimId);<br>        <span class="hljs-keyword">case</span> <span class="hljs-number">2</span> -&gt; claimQueryService.current(userId, orderId, requestId);<br>        <span class="hljs-keyword">case</span> <span class="hljs-number">0</span> -&gt; ClaimResponse.soldOut();<br>        <span class="hljs-keyword">default</span> -&gt; <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">IllegalArgumentException</span>(<span class="hljs-string">&quot;idempotency key conflict&quot;</span>);<br>    &#125;;<br>&#125;<br></code></pre></td></tr></table></figure><p>这段代码里有两个很关键的约束：</p><ul><li><code>requestId</code> 应该由客户端生成并在重试时保持不变，服务端还要校验请求体摘要，避免同一个键对应不同订单或不同参数；</li><li><code>userId</code> 必须从登录态中获取，不能直接相信请求体传入的用户 ID。</li></ul><p>实际项目可以把 Redis 调用封装成一个 <code>ClaimGateway</code>，统一负责 key 生成、脚本加载、超时、异常转换和埋点。业务层只关心 <code>ACCEPTED</code>、<code>ALREADY_ACCEPTED</code>、<code>SOLD_OUT</code>、<code>CONFLICT</code> 四种结果。</p><h2 id="七、消费者与-MySQL-写入"><a href="#七、消费者与-MySQL-写入" class="headerlink" title="七、消费者与 MySQL 写入"></a>七、消费者与 MySQL 写入</h2><p>Redis 裁决成功后，消费者从 Stream 读取事件。消费者应该使用 consumer group，而不是让每个实例自己扫描列表：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs text">XREADGROUP GROUP claim-group consumer-1<br>  BLOCK 2000 COUNT 200<br>  STREAMS claim:stream:&#123;p:12&#125; &gt;<br></code></pre></td></tr></table></figure><p>消费事件的数据库事务可以简化为：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-meta">@Transactional</span><br><span class="hljs-keyword">public</span> <span class="hljs-type">void</span> <span class="hljs-title function_">persist</span><span class="hljs-params">(ClaimEvent event)</span> &#123;<br>    <span class="hljs-type">int</span> <span class="hljs-variable">inserted</span> <span class="hljs-operator">=</span> claimMapper.insertIgnore(<br>        event.claimId(),<br>        event.orderId(),<br>        event.userId(),<br>        event.requestId()<br>    );<br><br>    <span class="hljs-keyword">if</span> (inserted == <span class="hljs-number">0</span>) &#123;<br>        <span class="hljs-type">ClaimRecord</span> <span class="hljs-variable">old</span> <span class="hljs-operator">=</span> claimMapper.findByOrderId(event.orderId());<br>        <span class="hljs-keyword">if</span> (old == <span class="hljs-literal">null</span> || !old.claimId().equals(event.claimId())) &#123;<br>            <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">IllegalStateException</span>(<span class="hljs-string">&quot;duplicate claim with different owner&quot;</span>);<br>        &#125;<br>        <span class="hljs-keyword">return</span>;<br>    &#125;<br><br>    <span class="hljs-type">int</span> <span class="hljs-variable">updated</span> <span class="hljs-operator">=</span> orderMapper.assign(<br>        event.orderId(),<br>        event.userId(),<br>        event.claimId()<br>    );<br><br>    <span class="hljs-keyword">if</span> (updated == <span class="hljs-number">0</span>) &#123;<br>        <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">IllegalStateException</span>(<span class="hljs-string">&quot;order already assigned&quot;</span>);<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>对应表结构：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><code class="hljs sql"><span class="hljs-keyword">CREATE TABLE</span> order_claim (<br>  id <span class="hljs-type">BIGINT</span> UNSIGNED <span class="hljs-keyword">NOT NULL</span>,<br>  order_id <span class="hljs-type">BIGINT</span> UNSIGNED <span class="hljs-keyword">NOT NULL</span>,<br>  user_id <span class="hljs-type">BIGINT</span> UNSIGNED <span class="hljs-keyword">NOT NULL</span>,<br>  request_id <span class="hljs-type">VARCHAR</span>(<span class="hljs-number">64</span>) <span class="hljs-keyword">NOT NULL</span>,<br>  claim_id <span class="hljs-type">VARCHAR</span>(<span class="hljs-number">64</span>) <span class="hljs-keyword">NOT NULL</span>,<br>  status TINYINT <span class="hljs-keyword">NOT NULL</span>,<br>  created_at DATETIME(<span class="hljs-number">3</span>) <span class="hljs-keyword">NOT NULL</span> <span class="hljs-keyword">DEFAULT</span> <span class="hljs-built_in">CURRENT_TIMESTAMP</span>(<span class="hljs-number">3</span>),<br>  updated_at DATETIME(<span class="hljs-number">3</span>) <span class="hljs-keyword">NOT NULL</span> <span class="hljs-keyword">DEFAULT</span> <span class="hljs-built_in">CURRENT_TIMESTAMP</span>(<span class="hljs-number">3</span>)<br>    <span class="hljs-keyword">ON</span> <span class="hljs-keyword">UPDATE</span> <span class="hljs-built_in">CURRENT_TIMESTAMP</span>(<span class="hljs-number">3</span>),<br>  <span class="hljs-keyword">PRIMARY KEY</span> (id),<br>  <span class="hljs-keyword">UNIQUE</span> KEY uk_order_id (order_id),<br>  <span class="hljs-keyword">UNIQUE</span> KEY uk_user_request (user_id, request_id),<br>  <span class="hljs-keyword">UNIQUE</span> KEY uk_claim_id (claim_id)<br>) ENGINE<span class="hljs-operator">=</span>InnoDB <span class="hljs-keyword">DEFAULT</span> CHARSET<span class="hljs-operator">=</span>utf8mb4;<br></code></pre></td></tr></table></figure><p><code>uk_order_id</code> 是最终防线的核心，它保证同一个订单在数据库中不可能出现两个有效赢家。即使 Redis 因故障恢复、重复投递或人工操作产生重复事件，数据库也会拒绝第二个赢家。</p><p>消费者的执行顺序必须是：</p><ol><li>读取事件；</li><li>开启 MySQL 事务并提交；</li><li>执行 Redis 的 <code>MARK_ASSIGNED</code> 脚本；</li><li>Redis 更新成功后 <code>XACK</code>；</li><li>发布“订单已归属”的下游事件。</li></ol><p><strong>不要先 ACK 再写数据库。</strong> 如果进程在两步之间崩溃，事件就永久丢失了。相反，数据库已经提交但 Redis 更新失败或 ACK 失败，事件会被重复消费，而数据库的唯一索引和 <code>claimId</code> 正好用于去重。</p><h3 id="批量库存模型怎么写"><a href="#批量库存模型怎么写" class="headerlink" title="批量库存模型怎么写"></a>批量库存模型怎么写</h3><p>如果抢的是活动库存，MySQL 侧通常写入一张 <code>grab_success</code> 表，至少包含：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs text">activity_id, user_id, request_id, claim_id<br>unique(activity_id, user_id)<br>unique(user_id, request_id)<br>unique(claim_id)<br></code></pre></td></tr></table></figure><p>Redis 侧把订单状态 Hash 换成活动库存 Hash：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs text">HSET activity:&#123;p:12&#125;:1001 stock 1000 status OPEN<br></code></pre></td></tr></table></figure><p>在 Lua 里先判断 <code>stock &gt; 0</code>，再原子执行 <code>HINCRBY stock -1</code> 并写入成功事件。不能先 <code>GET</code>、再在 Java 里判断、最后 <code>DECR</code>，因为这是两个独立命令，会在并发下超卖。</p><h2 id="八、幂等、重试和状态查询"><a href="#八、幂等、重试和状态查询" class="headerlink" title="八、幂等、重试和状态查询"></a>八、幂等、重试和状态查询</h2><p>抢单系统的请求重试非常常见：用户连续点击、移动网络重发、网关超时重试、消费者重复投递。幂等要分两层做：</p><ul><li><strong>请求层</strong>：<code>userId + orderId + requestId</code> 唯一，重复请求返回同一个 <code>claimId</code>；</li><li><strong>事件层</strong>：<code>claimId</code> 唯一，重复消息只允许执行一次业务副作用。</li></ul><p>状态查询接口要根据 <code>claimId</code> 返回同一个结果，而不是重新抢一次：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs text">GET /api/v1/claims/&#123;claimId&#125;<br><br>QUEUED     已受理，等待数据库确认<br>ASSIGNED   已确认归属<br>FAILED     裁决失败或补偿完成<br>UNKNOWN    系统超时，正在自动核对<br></code></pre></td></tr></table></figure><p><code>UNKNOWN</code> 不应被当成失败。很多线上事故不是“数据写错”，而是客户端看到超时后立即重新抢单，造成新的竞争。服务端应该让同一幂等键查询继续返回原状态，直到补偿任务给出最终结果。</p><h2 id="九、失败处理与补偿"><a href="#九、失败处理与补偿" class="headerlink" title="九、失败处理与补偿"></a>九、失败处理与补偿</h2><p>高并发系统一定要先设计失败，而不是只设计成功路径。</p><h3 id="1-Redis-主节点故障"><a href="#1-Redis-主节点故障" class="headerlink" title="1. Redis 主节点故障"></a>1. Redis 主节点故障</h3><p>Redis 故障时，客户端可能遇到超时或连接重置。此时客户端应使用原 <code>requestId</code> 重试；服务端无法确认上一次是否已受理时，返回 <code>503 + retryable=true</code>。不要在没有确认原请求状态时直接换新请求 ID，否则可能重复参与竞争。</p><p>生产环境建议：</p><ul><li>开启 AOF，至少使用 <code>everysec</code>，并根据业务损失容忍度评估是否使用更高持久化等级；</li><li>每个主节点配置副本，使用 Redis Cluster 而不是单点；</li><li>监控 <code>rdb_last_bgsave_status</code>、AOF 重写、主从延迟和 failover 时间；</li><li>将 Redis 当作裁决层而不是唯一事实源，数据库和补偿任务负责最终修复。</li></ul><h3 id="2-Stream-消费积压"><a href="#2-Stream-消费积压" class="headerlink" title="2. Stream 消费积压"></a>2. Stream 消费积压</h3><p>消费者数量不足、数据库变慢或通知系统故障都会让 Stream 堆积。需要监控：</p><ul><li>每个 Stream 的长度；</li><li>consumer group 的 <code>pending</code> 数量；</li><li>最老 pending 消息的等待时间；</li><li>消费失败率、重试次数和死信数量。</li></ul><p>积压时可以先横向扩容消费者，但不能无限扩张数据库连接。更稳妥的方式是按分区消费、批量提交，并在数据库端限制单实例并发。</p><h3 id="3-消费者重复或乱序"><a href="#3-消费者重复或乱序" class="headerlink" title="3. 消费者重复或乱序"></a>3. 消费者重复或乱序</h3><p>Redis Stream 的消费者可能重复拿到同一条消息。数据库事务和唯一索引必须能承受重复执行，处理逻辑要满足：</p><ul><li>同一个 <code>claimId</code> 重放不会增加库存；</li><li>同一个 <code>orderId</code> 只允许一个有效归属；</li><li>后到的旧事件不能覆盖新状态；</li><li>业务状态更新使用 <code>WHERE status = ?</code> 或版本号做条件更新。</li></ul><h3 id="4-超时释放要谨慎"><a href="#4-超时释放要谨慎" class="headerlink" title="4. 超时释放要谨慎"></a>4. 超时释放要谨慎</h3><p>不要把“超过 1 秒没有写数据库”直接等同于失败并释放订单。消费者可能只是暂时 GC、网络抖动或数据库慢查询。更安全的状态机是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs text">CLAIMING -&gt; 消费者处理中<br>CLAIMING -&gt; UNKNOWN -&gt; 补偿任务核对数据库和 Stream<br>UNKNOWN  -&gt; ASSIGNED<br>UNKNOWN  -&gt; FAILED<br>UNKNOWN  -&gt; RELEASED<br></code></pre></td></tr></table></figure><p>释放操作也要通过 Lua 比较 <code>claimId</code> 和当前状态：</p><figure class="highlight lua"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs lua"><span class="hljs-keyword">if</span> redis.call(<span class="hljs-string">&#x27;HGET&#x27;</span>, KEYS[<span class="hljs-number">1</span>], <span class="hljs-string">&#x27;claimId&#x27;</span>) ~= ARGV[<span class="hljs-number">1</span>] <span class="hljs-keyword">then</span><br>  <span class="hljs-keyword">return</span> <span class="hljs-number">0</span><br><span class="hljs-keyword">end</span><br></code></pre></td></tr></table></figure><p>否则一个已经成功提交的订单，可能被旧补偿任务错误释放，随后又被第二个用户抢走。数据库唯一索引仍是最后一道保险，但用户会看到一次错误的“抢到了”再失败，体验很差。</p><h2 id="十、限流、防刷和热点保护"><a href="#十、限流、防刷和热点保护" class="headerlink" title="十、限流、防刷和热点保护"></a>十、限流、防刷和热点保护</h2><p>网关层至少要有按用户、设备、IP 和接口维度的令牌桶限流。抢单接口可以配置为：</p><ul><li>单用户单订单每秒 1 次；</li><li>单用户全局限速，避免脚本批量点击；</li><li>单 IP、单设备并发的宽松阈值，主要拦截异常流量；</li><li>全局入口限流，保护 Redis 和下游数据库。</li></ul><p>限流器可以用 Redis 令牌桶 Lua 脚本实现，但在 Redis 故障时不能直接放行所有请求。更合理的降级是切换到本地 Caffeine 限流器，并降低全局入口配额。</p><p>还需要做：</p><ul><li>登录态校验，用户 ID 只从服务端会话或 JWT 中取；</li><li>请求签名或一次性挑战，增加脚本伪造成本；</li><li>画像、设备指纹和频控规则，异常时触发验证码或排队页；</li><li>订单详情和活动信息走 CDN&#x2F;本地缓存，避免所有流量集中打到数据库。</li></ul><p>热点保护的关键不是把同一个订单拆成多个虚拟库存，而是尽快在 Redis 层拒绝失败请求，只让有限的成功事件进入数据库。</p><h2 id="十一、可观测性与测试"><a href="#十一、可观测性与测试" class="headerlink" title="十一、可观测性与测试"></a>十一、可观测性与测试</h2><p>每一条链路都要能回答两个问题：<strong>这个 claimId 现在在哪里？为什么还没成功？</strong></p><p>建议埋点：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs text">claim_requests_total&#123;result&#125;<br>claim_lua_duration_seconds<br>claim_stream_length<br>claim_stream_pending<br>claim_consumer_retry_total<br>claim_db_duration_seconds<br>claim_assigned_total<br>claim_repair_total<br>claim_unknown_total<br></code></pre></td></tr></table></figure><p>日志中至少带上 <code>traceId</code>、<code>claimId</code>、<code>orderId</code>、<code>userId</code>、<code>requestId</code>、状态和重试次数。不要把完整手机号、身份证号或敏感业务参数写进日志。</p><p>测试不能只做单接口压测，还要覆盖正确性：</p><ol><li>使用 Gatling 或 k6 模拟 10 万并发，抢 5 千个订单；</li><li>故意让 30% 请求使用相同幂等键重复发送；</li><li>压测后校验每单只有一个 <code>ASSIGNED</code>，成功数不超过库存；</li><li>验证重复消息、消费者重启和 Redis 主从切换后数据仍然正确；</li><li>注入 MySQL 延迟、消费者暂停、网络丢包，观察 backlog 是否最终收敛；</li><li>统计 Redis evalsha P99、Stream pending、数据库写入 P99 和补偿数量。</li></ol><p>一个简单的数据校验 SQL：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs sql"><span class="hljs-keyword">SELECT</span> order_id, <span class="hljs-built_in">COUNT</span>(<span class="hljs-operator">*</span>)<br><span class="hljs-keyword">FROM</span> order_claim<br><span class="hljs-keyword">WHERE</span> status <span class="hljs-operator">=</span> <span class="hljs-number">10</span><br><span class="hljs-keyword">GROUP</span> <span class="hljs-keyword">BY</span> order_id<br><span class="hljs-keyword">HAVING</span> <span class="hljs-built_in">COUNT</span>(<span class="hljs-operator">*</span>) <span class="hljs-operator">&gt;</span> <span class="hljs-number">1</span>;<br><br><span class="hljs-keyword">SELECT</span> <span class="hljs-built_in">COUNT</span>(<span class="hljs-operator">*</span>)<br><span class="hljs-keyword">FROM</span> order_claim<br><span class="hljs-keyword">WHERE</span> status <span class="hljs-operator">=</span> <span class="hljs-number">10</span>;<br></code></pre></td></tr></table></figure><p>第一条必须始终为空，第二条的规定应小于或等于可抢订单数。</p><h2 id="十二、演进路线与落地清单"><a href="#十二、演进路线与落地清单" class="headerlink" title="十二、演进路线与落地清单"></a>十二、演进路线与落地清单</h2><p>如果项目刚开始，不要一开始就上最复杂的分布式事务。可以按下面顺序演进：</p><ol><li>用 Redis Lua 完成原子裁决和幂等；</li><li>用 Redis Stream 串行化成功事件，并让消费者写 MySQL；</li><li>加数据库唯一索引、补偿任务和状态查询；</li><li>当流量或合规要求提高后，把 Stream 替换为 Kafka&#x2F;RocketMQ，并通过 outbox 或中继保证投递；</li><li>最后再做多地域、多集群和更细粒度的容量治理。</li></ol><p>上线前逐项检查：</p><ul><li><input disabled="" type="checkbox"> 单资源是否有数据库唯一约束？</li><li><input disabled="" type="checkbox"> 批量库存是否在 Lua 中原子扣减？</li><li><input disabled="" type="checkbox"> 是否禁止在热点路径上使用 Redis 分布式锁？</li><li><input disabled="" type="checkbox"> 幂等键是否覆盖用户、订单和请求？</li><li><input disabled="" type="checkbox"> 消费者是否在数据库提交后才 ACK？</li><li><input disabled="" type="checkbox"> 是否能在不重新抢单的情况下查询原 claimId？</li><li><input disabled="" type="checkbox"> Redis 故障、MySQL 变慢、消费者重启时是否有降级和补偿？</li><li><input disabled="" type="checkbox"> 是否有限流、防刷、鉴权和请求体校验？</li><li><input disabled="" type="checkbox"> 是否有 P99、pending、补偿次数和最终归属的监控？</li><li><input disabled="" type="checkbox"> 是否用压测和故障注入验证过“不重复、不超卖”？</li></ul><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>高并发抢单的核心不是把数据库优化到极限，而是把系统拆成三层：</p><p><strong>Redis 做瞬时、原子、可重放的裁决；消息系统做削峰和可靠传递；MySQL 做最终事实和唯一性兜底。</strong></p><p>Java 服务保持无状态，Lua 脚本只处理最小状态转移，消费者在事务提交后再更新状态和 ACK，补偿任务负责修复不确定结果。做到这些之后，即使面对几十万 QPS、网络抖动和节点故障，系统仍然可以保持“同一个订单最终只有一个赢家”。</p>]]></content>
    
    
    <categories>
      
      <category>后端</category>
      
    </categories>
    
    
    <tags>
      
      <tag>Java</tag>
      
      <tag>Spring Boot</tag>
      
      <tag>Redis</tag>
      
      <tag>MySQL</tag>
      
      <tag>高并发</tag>
      
      <tag>抢单</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>博客重构实录：从手写 HTML 到 Hexo + Fluid</title>
    <link href="/2026/09/13/blog-rebuild-log/"/>
    <url>/2026/09/13/blog-rebuild-log/</url>
    
    <content type="html"><![CDATA[<p>这篇记录一次完整的博客重构：换主题、升级环境，以及推送时踩到的坑。涉及的东西比较杂，按时间顺序写，方便以后翻回来查。</p><span id="more"></span><h2 id="一、起点：一个半成品的手写站点"><a href="#一、起点：一个半成品的手写站点" class="headerlink" title="一、起点：一个半成品的手写站点"></a>一、起点：一个半成品的手写站点</h2><p>重构前的仓库里只有一个手写的 <code>index.html</code>，问题不少：</p><ul><li>页面标题还停留在模板阶段的 “JavaScript Crash Course”；</li><li>正文是反复粘贴的 Lorem ipsum 占位文字，侧边栏是空的；</li><li>导航里的“科学研究 &#x2F; 研究人员 &#x2F; 联系我们”三个页面根本不存在，点进去就是 404；</li><li><code>index.js</code> 里是一段数组遍历的练习代码，和页面没有任何关系；</li><li>样式表是半成品，固定顶栏和正文还会重叠。</li></ul><p>与其继续缝补，不如换成成熟的静态博客方案：写作用 Markdown，生成交给工具，样式交给主题。</p><h2 id="二、选主题：为什么是-Fluid"><a href="#二、选主题：为什么是-Fluid" class="headerlink" title="二、选主题：为什么是 Fluid"></a>二、选主题：为什么是 Fluid</h2><p>几个热门主题对比下来：</p><table><thead><tr><th>主题</th><th>特点</th><th>是否合适</th></tr></thead><tbody><tr><td>NexT</td><td>老牌、生态最大，风格朴素，配置项偏多</td><td>稳，但观感偏“学院风”</td></tr><tr><td>Butterfly</td><td>功能最全、国内用户多，卡片和动效密集</td><td>功能有余，简约不足</td></tr><tr><td><strong>Fluid</strong></td><td>Material Design，留白干净，自带暗色模式和本地搜索</td><td>✅ 最终选择</td></tr></tbody></table><p>Fluid 的中文文档完善、仍在活跃维护，整体观感是“简约大气”，符合这次的目标。</p><h2 id="三、升级-Node：14-15-→-24-LTS"><a href="#三、升级-Node：14-15-→-24-LTS" class="headerlink" title="三、升级 Node：14.15 → 24 LTS"></a>三、升级 Node：14.15 → 24 LTS</h2><p>原来机器上是 Node 14（2020 年的版本），而最新版 Hexo 要求 Node ≥ 20，先升级环境。</p><h3 id="1-先摸清现状"><a href="#1-先摸清现状" class="headerlink" title="1. 先摸清现状"></a>1. 先摸清现状</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs bash">node -v                 <span class="hljs-comment"># v14.15.0</span><br>npm -v                  <span class="hljs-comment"># 6.14.8</span><br>npm config get prefix   <span class="hljs-comment"># D:\appFile\node\node_global</span><br></code></pre></td></tr></table></figure><p>从目录结构看，Node 是绿色版解压安装（目录里有 <code>node.exe</code>、<code>npm.cmd</code>、<code>nodevars.bat</code>），所以直接替换 zip 内容即可，不用动 PATH。</p><h3 id="2-下载、校验、原地替换"><a href="#2-下载、校验、原地替换" class="headerlink" title="2. 下载、校验、原地替换"></a>2. 下载、校验、原地替换</h3><figure class="highlight powershell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs powershell"><span class="hljs-comment"># 下载并校验（SHA256 必须和 SHASUMS256.txt 一致）</span><br><span class="hljs-built_in">Invoke-WebRequest</span> https://nodejs.org/dist/index.json <span class="hljs-literal">-OutFile</span> index.json<br><span class="hljs-comment"># …下载 node-v24.x.x-win-x64.zip，比对哈希后解压</span><br><br><span class="hljs-comment"># 备份旧版本，再放入新版本</span><br><span class="hljs-built_in">Move-Item</span> D:\appFile\node D:\appFile\node<span class="hljs-literal">-v14</span>.<span class="hljs-number">15.0</span><span class="hljs-literal">-backup</span><br><span class="hljs-built_in">Copy-Item</span> .\extract\node<span class="hljs-literal">-v24</span>.x.x<span class="hljs-literal">-win-x64</span>\* D:\appFile\node <span class="hljs-literal">-Recurse</span><br></code></pre></td></tr></table></figure><p>因为安装目录没变，PATH、全局包目录（<code>node_global</code>）、npm 缓存都继续可用。</p><h3 id="3-踩到的坑：npm-版本还是旧的"><a href="#3-踩到的坑：npm-版本还是旧的" class="headerlink" title="3. 踩到的坑：npm 版本还是旧的"></a>3. 踩到的坑：npm 版本还是旧的</h3><p>换完之后 <code>node -v</code> 已经是 24，但 <code>npm -v</code> 却仍然输出 <code>6.14.8</code>。原因是新版 <code>npm.cmd</code> 的逻辑是：</p><figure class="highlight bat"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs bat"><span class="hljs-keyword">FOR</span> /F … <span class="hljs-keyword">IN</span> (&#x27;<span class="hljs-keyword">CALL</span> &quot;<span class="hljs-variable">%NODE_EXE%</span>&quot; &quot;<span class="hljs-variable">%NPM_PREFIX_JS%</span>&quot;&#x27;) <span class="hljs-keyword">DO</span> <span class="hljs-built_in">SET</span> &quot;NPM_PREFIX_NPM_CLI_JS=<span class="hljs-variable">%%F</span>\node_modules\npm\bin\npm-cli.js&quot;<br><span class="hljs-keyword">IF</span> <span class="hljs-keyword">EXIST</span> &quot;<span class="hljs-variable">%NPM_PREFIX_NPM_CLI_JS%</span>&quot; <span class="hljs-built_in">SET</span> &quot;NPM_CLI_JS=<span class="hljs-variable">%NPM_PREFIX_NPM_CLI_JS%</span>&quot;<br></code></pre></td></tr></table></figure><p>也就是说，它会优先使用<strong>全局 prefix 目录下安装的 npm</strong>。而 <code>node_global/node_modules/npm</code> 里还留着当年 <code>npm i -g npm</code> 装下的 6.14.8，于是 Node 换了、npm 没换。</p><p>处理办法很简单：把那个遗留的全局 npm 及其 shim 移出 <code>node_global</code>，让 npm 回落到 Node 自带的版本。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs bash">node -v   <span class="hljs-comment"># v24.21.0</span><br>npm  -v   <span class="hljs-comment"># 11.19.0</span><br></code></pre></td></tr></table></figure><h2 id="四、搭-Hexo-工程"><a href="#四、搭-Hexo-工程" class="headerlink" title="四、搭 Hexo 工程"></a>四、搭 Hexo 工程</h2><h3 id="1-版本组合"><a href="#1-版本组合" class="headerlink" title="1. 版本组合"></a>1. 版本组合</h3><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs json"><span class="hljs-punctuation">&#123;</span><br>  <span class="hljs-attr">&quot;dependencies&quot;</span><span class="hljs-punctuation">:</span> <span class="hljs-punctuation">&#123;</span><br>    <span class="hljs-attr">&quot;hexo&quot;</span><span class="hljs-punctuation">:</span> <span class="hljs-string">&quot;^8.1.2&quot;</span><span class="hljs-punctuation">,</span><br>    <span class="hljs-attr">&quot;hexo-theme-fluid&quot;</span><span class="hljs-punctuation">:</span> <span class="hljs-string">&quot;^1.9.9&quot;</span><span class="hljs-punctuation">,</span><br>    <span class="hljs-attr">&quot;hexo-server&quot;</span><span class="hljs-punctuation">:</span> <span class="hljs-string">&quot;^3.0.0&quot;</span><span class="hljs-punctuation">,</span><br>    <span class="hljs-attr">&quot;hexo-renderer-marked&quot;</span><span class="hljs-punctuation">:</span> <span class="hljs-string">&quot;^7.0.1&quot;</span><span class="hljs-punctuation">,</span><br>    <span class="hljs-attr">&quot;hexo-renderer-stylus&quot;</span><span class="hljs-punctuation">:</span> <span class="hljs-string">&quot;^3.0.1&quot;</span><br>  <span class="hljs-punctuation">&#125;</span><br><span class="hljs-punctuation">&#125;</span><br></code></pre></td></tr></table></figure><h3 id="2-让-Pages-直接从-docs-目录发布"><a href="#2-让-Pages-直接从-docs-目录发布" class="headerlink" title="2. 让 Pages 直接从 docs 目录发布"></a>2. 让 Pages 直接从 docs 目录发布</h3><p>站点配置里指定生成目录：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs yaml"><span class="hljs-attr">public_dir:</span> <span class="hljs-string">docs</span><br></code></pre></td></tr></table></figure><p>这样生成结果提交到仓库的 <code>docs/</code>，GitHub Pages 的发布源选 <code>master</code> + <code>/docs</code> 就能直接上线，源文件和生成结果放在同一个分支，日常维护最简单。</p><h3 id="3-主题配置的覆盖方式"><a href="#3-主题配置的覆盖方式" class="headerlink" title="3. 主题配置的覆盖方式"></a>3. 主题配置的覆盖方式</h3><p>主题自带一份 <code>_config.yml</code>，直接改它，升级主题时会被覆盖。正确做法是在站点根目录建 <code>_config.fluid.yml</code>：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs yaml"><span class="hljs-attr">navbar:</span><br>  <span class="hljs-attr">blog_title:</span> <span class="hljs-string">&quot;&quot;</span><br>  <span class="hljs-attr">menu:</span><br>    <span class="hljs-bullet">-</span> &#123; <span class="hljs-attr">key:</span> <span class="hljs-string">&quot;home&quot;</span>, <span class="hljs-attr">link:</span> <span class="hljs-string">&quot;/&quot;</span>, <span class="hljs-attr">icon:</span> <span class="hljs-string">&quot;iconfont icon-home-fill&quot;</span> &#125;<br>    <span class="hljs-bullet">-</span> &#123; <span class="hljs-attr">key:</span> <span class="hljs-string">&quot;archive&quot;</span>, <span class="hljs-attr">link:</span> <span class="hljs-string">&quot;/archives/&quot;</span>, <span class="hljs-attr">icon:</span> <span class="hljs-string">&quot;iconfont icon-archive-fill&quot;</span> &#125;<br></code></pre></td></tr></table></figure><p>Hexo 会加载 <code>_config.&lt;主题名&gt;.yml</code>，再和主题默认配置深度合并，只有写到的字段被覆盖。</p><p>顺带一个发现：Fluid 1.9.9 自带搜索索引生成器（<code>scripts/generators/local-search.js</code>），所以不需要额外装 <code>hexo-generator-searchdb</code>，装了反而会多生成一份索引。</p><h3 id="4-必须加的-nojekyll"><a href="#4-必须加的-nojekyll" class="headerlink" title="4. 必须加的 .nojekyll"></a>4. 必须加的 .nojekyll</h3><p>这是最容易忽略、后果最严重的一个点。</p><p>GitHub Pages 用“分支 + 目录”发布时，默认会先跑一遍 Jekyll。而 Fluid 的搜索模板 <code>source/xml/local-search.xml</code> 会被原样复制到产物里，里面是 Nunjucks 语法：</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs xml">&#123;% for post in posts.toArray() %&#125;<br><span class="hljs-tag">&lt;<span class="hljs-name">link</span> <span class="hljs-attr">href</span>=<span class="hljs-string">&quot;&#123;&#123; [url, post.path] | urlJoin | uriencode &#125;&#125;&quot;</span>/&gt;</span><br>&#123;% endfor %&#125;<br></code></pre></td></tr></table></figure><p>Jekyll 会把它当 Liquid 模板解析，<code>urlJoin</code>、<code>noControlChars</code> 这些过滤器并不存在，构建就会失败。</p><p>解决办法是在产物根目录放一个 <code>.nojekyll</code> 文件，让 Pages 跳过 Jekyll。为了不被 <code>hexo clean</code> 清掉，用生成器自动产出：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs js"><span class="hljs-comment">// scripts/nojekyll.js</span><br>hexo.<span class="hljs-property">extend</span>.<span class="hljs-property">generator</span>.<span class="hljs-title function_">register</span>(<span class="hljs-string">&#x27;nojekyll&#x27;</span>, <span class="hljs-function">() =&gt;</span> (&#123;<br>  <span class="hljs-attr">path</span>: <span class="hljs-string">&#x27;.nojekyll&#x27;</span>,<br>  <span class="hljs-attr">data</span>: <span class="hljs-string">&#x27;&#x27;</span><br>&#125;));<br></code></pre></td></tr></table></figure><h2 id="五、推送踩的三个坑"><a href="#五、推送踩的三个坑" class="headerlink" title="五、推送踩的三个坑"></a>五、推送踩的三个坑</h2><h3 id="坑-1：凭据失效"><a href="#坑-1：凭据失效" class="headerlink" title="坑 1：凭据失效"></a>坑 1：凭据失效</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs text">remote: Invalid username or token. Password authentication is not supported for Git operations.<br></code></pre></td></tr></table></figure><p>GitHub 早已不支持密码推送，本机的凭据管理器里也没有有效凭据。用 Git Credential Manager 重新登录即可：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs bash">git-credential-manager github login          <span class="hljs-comment"># 浏览器授权</span><br>git-credential-manager github login --device <span class="hljs-comment"># 或设备码方式</span><br></code></pre></td></tr></table></figure><h3 id="坑-2：github-com-的-443-连不上"><a href="#坑-2：github-com-的-443-连不上" class="headerlink" title="坑 2：github.com 的 443 连不上"></a>坑 2：github.com 的 443 连不上</h3><p>授权完成后推送，卡在连接阶段：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs text">fatal: unable to access &#x27;https://github.com/...&#x27;: Failed to connect to github.com port 443<br></code></pre></td></tr></table></figure><p>一步步排查：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs bash">nslookup github.com                                     <span class="hljs-comment"># 解析正常</span><br>curl -o NUL -w <span class="hljs-string">&quot;%&#123;http_code&#125;&quot;</span> https://api.github.com/   <span class="hljs-comment"># 200</span><br>curl -o NUL -w <span class="hljs-string">&quot;%&#123;http_code&#125;&quot;</span> https://raw.githubusercontent.com/  <span class="hljs-comment"># 301</span><br>curl -o NUL -w <span class="hljs-string">&quot;%&#123;http_code&#125;&quot;</span> https://github.com/       <span class="hljs-comment"># 000（超时）</span><br></code></pre></td></tr></table></figure><p>结论很明确：不是 DNS 问题，也不是凭据问题，而是<strong>访问 github.com 这个域名的 443 端口恰好不通</strong>，而同一台机器访问 GitHub 的其它域名、其它 IP 都正常。</p><p>临时绕过的思路：在本地起一个极小的 HTTP CONNECT 转发，把 git 发往 <code>github.com:443</code> 的连接转发到一个可用的 GitHub IP，然后让 git 走这个本地代理。</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs js"><span class="hljs-comment">// 思路示意：不转发域名，直接连一个已验证可达的 IP</span><br><span class="hljs-keyword">const</span> upstream = net.<span class="hljs-title function_">connect</span>(<span class="hljs-number">443</span>, <span class="hljs-string">&#x27;可用的 GitHub IP&#x27;</span>, <span class="hljs-function">() =&gt;</span> &#123;<br>  client.<span class="hljs-title function_">write</span>(<span class="hljs-string">&#x27;HTTP/1.1 200 Connection Established\r\n\r\n&#x27;</span>);<br>  client.<span class="hljs-title function_">pipe</span>(upstream);<br>  upstream.<span class="hljs-title function_">pipe</span>(client);<br>&#125;);<br></code></pre></td></tr></table></figure><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">git -c http.proxy=http://127.0.0.1:8899 push origin master<br></code></pre></td></tr></table></figure><p>推送成功后立刻停掉进程、删掉脚本。这只是一次性的救急手段，<strong>不建议长期使用</strong>：IP 会变，进程要常驻，也不是正经的代理。长期方案是改用 SSH（GitHub 的 22 &#x2F; 443 端口都通），或者给 git 配上自己的代理。</p><h3 id="坑-3：远端历史完全对不上"><a href="#坑-3：远端历史完全对不上" class="headerlink" title="坑 3：远端历史完全对不上"></a>坑 3：远端历史完全对不上</h3><p>推送被拒：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs text">! [rejected] master -&gt; master (fetch first)<br></code></pre></td></tr></table></figure><p><code>git fetch</code> 之后才发现问题所在：远端 <code>master</code> 上是 2022 年部署的另一个站点（Icarus 主题的 “CMY’s Blog”），和本地历史<strong>没有共同祖先</strong>。本地这份拷贝是很久以前克隆的，远端早已被换成别的内容。</p><p>处理方式：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs bash">git branch backup/cmy-blog-2022 origin/master   <span class="hljs-comment"># 旧内容先在本地留个分支</span><br>git push --force-with-lease origin master        <span class="hljs-comment"># 用 --force-with-lease 而不是 --force</span><br></code></pre></td></tr></table></figure><p>用 <code>--force-with-lease</code> 而不是 <code>--force</code>，可以在远端被他人更新时主动失败，避免误覆盖别人的提交。</p><h2 id="六、Pages-构建与缓存"><a href="#六、Pages-构建与缓存" class="headerlink" title="六、Pages 构建与缓存"></a>六、Pages 构建与缓存</h2><p>推送后站点没变，查了构建记录（<code>GET /repos/{owner}/{repo}/pages/builds/latest</code>）才看清过程：</p><ul><li>第一次构建 <code>errored</code>；</li><li>紧接着的第二次构建 <code>built</code>（18 秒）；</li><li>但浏览器里看到的仍是旧页面。</li></ul><p>前两条说明构建系统会自动重试，不用手动干预；最后一条是<strong>CDN 缓存</strong>——GitHub Pages 的 HTML 缓存大约 10 分钟，加个查询参数或强制刷新就能看到最新内容。</p><h2 id="七、经验清单"><a href="#七、经验清单" class="headerlink" title="七、经验清单"></a>七、经验清单</h2><ul><li>换 Node 时用绿色版 zip 原地替换最省事，但记得检查 <code>node_global</code> 里有没有遗留的全局 npm 之类会“抢先”的包；</li><li>主题配置一律写在根目录的 <code>_config.&lt;theme&gt;.yml</code>，不要改 <code>node_modules</code> 里的文件；</li><li>用“分支 + 目录”发布 Pages 时，产物根目录一定要有 <code>.nojekyll</code>；</li><li>推送失败先分辨是<strong>凭据问题</strong>、<strong>网络问题</strong>还是<strong>历史冲突</strong>，三者的报错和解决方式完全不同；</li><li>覆盖远端前先 <code>fetch</code> 看清远端到底有什么，并用 <code>--force-with-lease</code> + 本地备份分支；</li><li>线上没更新不代表没部署成功，先看构建记录，再考虑缓存。</li></ul><h2 id="八、待办"><a href="#八、待办" class="headerlink" title="八、待办"></a>八、待办</h2><ul><li><input disabled="" type="checkbox"> 配置 SSH 推送，摆脱对临时转发和 IP 的依赖；</li><li><input disabled="" type="checkbox"> 把头像和关于页换成真实信息；</li><li><input disabled="" type="checkbox"> 试试给文章加上评论（giscus）和访问统计。</li></ul>]]></content>
    
    
    <categories>
      
      <category>博客</category>
      
    </categories>
    
    
    <tags>
      
      <tag>Hexo</tag>
      
      <tag>Fluid</tag>
      
      <tag>GitHub Pages</tag>
      
      <tag>Node.js</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>用 Hexo + Fluid 重建本站</title>
    <link href="/2026/09/13/hello-hexo/"/>
    <url>/2026/09/13/hello-hexo/</url>
    
    <content type="html"><![CDATA[<p>本站从手写的 HTML 页面迁移到了 <strong>Hexo</strong>，主题选用 <strong>Fluid</strong>。这篇是第一篇文章，用来记录建站过程，也顺便当作写作模板。</p><span id="more"></span><h2 id="为什么是-Hexo-Fluid"><a href="#为什么是-Hexo-Fluid" class="headerlink" title="为什么是 Hexo + Fluid"></a>为什么是 Hexo + Fluid</h2><p>选型时主要考虑三点：</p><ol><li><strong>纯静态生成</strong> —— 生成结果是普通 HTML&#x2F;CSS&#x2F;JS，天然适合 GitHub Pages，不需要服务器和数据库。</li><li><strong>写作即 Markdown</strong> —— 新文章就是一个 <code>.md</code> 文件，写完提交，站点自动更新。</li><li><strong>主题成熟</strong> —— <a href="https://hexo.fluid-dev.com/">Fluid</a> 是 Hexo 社区最流行的主题之一，界面简洁大气，自带暗色模式、本地搜索、响应式布局，中文文档也很完善。</li></ol><h2 id="目录结构"><a href="#目录结构" class="headerlink" title="目录结构"></a>目录结构</h2><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><code class="hljs text">.<br>├── _config.yml          # 站点配置（标题、作者、插件等）<br>├── _config.fluid.yml    # 主题配置（外观、菜单、社交链接等）<br>├── source/<br>│   ├── _posts/          # 文章，写作都在这里<br>│   ├── _drafts/         # 草稿，不会被发布<br>│   ├── about/           # 关于页<br>│   ├── categories/      # 分类页<br>│   ├── tags/            # 标签页<br>│   ├── css/             # 自定义样式<br>│   └── img/             # 图片资源<br>├── scaffolds/           # 新建文章/页面的模板<br>└── docs/                # 生成的静态站点（GitHub Pages 发布目录）<br></code></pre></td></tr></table></figure><h2 id="日常写作流程"><a href="#日常写作流程" class="headerlink" title="日常写作流程"></a>日常写作流程</h2><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-comment"># 新建一篇文章</span><br>npm run new -- <span class="hljs-string">&quot;我的第一篇文章&quot;</span><br><br><span class="hljs-comment"># 本地预览，浏览器打开 http://localhost:4000</span><br>npm run dev<br><br><span class="hljs-comment"># 生成静态站点到 docs/</span><br>npm run build<br><br><span class="hljs-comment"># 生成并推送到 GitHub，稍等片刻线上就更新了</span><br>npm run publish<br></code></pre></td></tr></table></figure><h2 id="写作之外的小事"><a href="#写作之外的小事" class="headerlink" title="写作之外的小事"></a>写作之外的小事</h2><ul><li>草稿放在 <code>source/_drafts/</code>，<code>npm run dev</code> 可以看到，正式生成时会忽略；</li><li>图片统一放 <code>source/img/</code>，文章里用 <code>/img/xxx.png</code> 引用；</li><li>想在首页显示摘要，在正文里加一行 <code>&lt;!-- more --&gt;</code>。</li></ul><p>就写到这里，接下来可以开始记录技术笔记了。</p>]]></content>
    
    
    <categories>
      
      <category>博客</category>
      
    </categories>
    
    
    <tags>
      
      <tag>Hexo</tag>
      
      <tag>Fluid</tag>
      
      <tag>GitHub Pages</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Hexo 常用命令速查</title>
    <link href="/2026/09/12/hexo-cheatsheet/"/>
    <url>/2026/09/12/hexo-cheatsheet/</url>
    
    <content type="html"><![CDATA[<p>把 Hexo 日常会用到的命令整理成一张表，方便随时查阅。</p><span id="more"></span><h2 id="命令速查"><a href="#命令速查" class="headerlink" title="命令速查"></a>命令速查</h2><table><thead><tr><th>命令</th><th>作用</th></tr></thead><tbody><tr><td><code>npm run new -- &quot;标题&quot;</code></td><td>新建文章（生成到 <code>source/_posts/</code>）</td></tr><tr><td><code>npm run new -- &quot;标题&quot; --draft</code></td><td>新建草稿（生成到 <code>source/_drafts/</code>）</td></tr><tr><td><code>npm run dev</code></td><td>本地预览，包含草稿，默认 <a href="http://localhost:4000/">http://localhost:4000</a></td></tr><tr><td><code>npm run build</code></td><td>生成静态站点到 <code>docs/</code></td></tr><tr><td><code>npm run clean</code></td><td>清除缓存和已生成的文件</td></tr><tr><td><code>npm run publish</code></td><td>重新生成并推送到 GitHub</td></tr></tbody></table><h2 id="文章头信息"><a href="#文章头信息" class="headerlink" title="文章头信息"></a>文章头信息</h2><p>每篇文章顶部的 <code>---</code> 之间是 Front-matter，常用字段：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs yaml"><span class="hljs-meta">---</span><br><span class="hljs-attr">title:</span> <span class="hljs-string">文章标题</span><br><span class="hljs-attr">date:</span> <span class="hljs-number">2026-09-13 10:00:00</span><br><span class="hljs-attr">categories:</span><br>  <span class="hljs-bullet">-</span> <span class="hljs-string">分类名</span><br><span class="hljs-attr">tags:</span><br>  <span class="hljs-bullet">-</span> <span class="hljs-string">标签一</span><br>  <span class="hljs-bullet">-</span> <span class="hljs-string">标签二</span><br><span class="hljs-meta">---</span><br></code></pre></td></tr></table></figure><h2 id="常见问题"><a href="#常见问题" class="headerlink" title="常见问题"></a>常见问题</h2><p><strong>改了配置没生效？</strong> 先 <code>npm run clean</code> 再重新生成，Hexo 会缓存渲染结果。</p><p><strong>文章没出现在首页？</strong> 检查 <code>date</code> 是否写成了未来的时间，默认情况下未来文章不会生成。</p><p><strong>想调整首页每页文章数？</strong> 修改 <code>_config.yml</code> 里的 <code>index_generator.per_page</code>。</p>]]></content>
    
    
    <categories>
      
      <category>工具</category>
      
    </categories>
    
    
    <tags>
      
      <tag>Hexo</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Markdown 写作示例</title>
    <link href="/2026/09/11/markdown-demo/"/>
    <url>/2026/09/11/markdown-demo/</url>
    
    <content type="html"><![CDATA[<p>这篇用来展示常见的 Markdown 写法在站点上的渲染效果，写文章时可以直接参考。</p><span id="more"></span><h2 id="标题与段落"><a href="#标题与段落" class="headerlink" title="标题与段落"></a>标题与段落</h2><p>正文直接写即可，段落之间空一行。<strong>加粗</strong>、<em>斜体</em>、<del>删除线</del>、<code>行内代码</code>。</p><h2 id="列表"><a href="#列表" class="headerlink" title="列表"></a>列表</h2><p>无序列表：</p><ul><li>第一项</li><li>第二项<ul><li>嵌套项</li></ul></li></ul><p>有序列表：</p><ol><li>第一步</li><li>第二步</li></ol><h2 id="引用与链接"><a href="#引用与链接" class="headerlink" title="引用与链接"></a>引用与链接</h2><blockquote><p>好的文档应该让读者一次读懂，而不是读第二遍。</p></blockquote><p>链接写法：<a href="https://hexo.io/zh-cn/docs/">Hexo 官方文档</a>。</p><h2 id="代码块"><a href="#代码块" class="headerlink" title="代码块"></a>代码块</h2><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs javascript"><span class="hljs-keyword">const</span> posts = [<span class="hljs-string">&#x27;写作&#x27;</span>, <span class="hljs-string">&#x27;阅读&#x27;</span>, <span class="hljs-string">&#x27;思考&#x27;</span>];<br><br>posts.<span class="hljs-title function_">forEach</span>(<span class="hljs-function">(<span class="hljs-params">item, index</span>) =&gt;</span> &#123;<br>  <span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(<span class="hljs-string">`<span class="hljs-subst">$&#123;index + <span class="hljs-number">1</span>&#125;</span>. <span class="hljs-subst">$&#123;item&#125;</span>`</span>);<br>&#125;);<br></code></pre></td></tr></table></figure><h2 id="表格"><a href="#表格" class="headerlink" title="表格"></a>表格</h2><table><thead><tr><th>语法</th><th>效果</th></tr></thead><tbody><tr><td><code>**文字**</code></td><td><strong>加粗</strong></td></tr><tr><td><code>*文字*</code></td><td><em>斜体</em></td></tr><tr><td><code>`代码`</code></td><td><code>代码</code></td></tr></tbody></table><h2 id="图片"><a href="#图片" class="headerlink" title="图片"></a>图片</h2><p>把图片放进 <code>source/img/</code>，然后这样引用：</p><figure class="highlight markdown"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs markdown">![<span class="hljs-string">图片说明</span>](<span class="hljs-link">/img/example.png</span>)<br></code></pre></td></tr></table></figure>]]></content>
    
    
    <categories>
      
      <category>写作</category>
      
    </categories>
    
    
    <tags>
      
      <tag>Markdown</tag>
      
    </tags>
    
  </entry>
  
  
  
  
</search>
